---
swagger: "2.0"
info:
  description: "OBFN Configuration API"
  version: "0.0.1"
  title: "OBFN API"
host: "localhost:5002"
basePath: "/api"
schemes:
- "http"
paths:
  /obfn:
    get:
      summary: "Retrieve configuration"
      description: "Retrieve operations of resource: beams"
      operationId: "retrieve_configuration"
      produces:
      - "application/json"
      parameters: []
      responses:
        200:
          description: "Successful operation"
          schema:
            $ref: "#/definitions/Operations"
        400:
          description: "Internal Error"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
    post:
      summary: "Create configuration"
      description: "Create operations of resource: beams"
      operationId: "create_configuration"
      produces:
      - "application/json"
      parameters:
      - in: "body"
        name: "body"
        description: "operations"
        required: true
        schema:
          $ref: "#/definitions/Operations"
      responses:
        200:
          description: "Successful operation"
          schema:
            $ref: "#/definitions/Operations"
        400:
          description: "Invalid parameters supplied"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
    delete:
      summary: "Delete configuration"
      description: "Delete operations of resource: beams"
      operationId: "delete_configuration"
      produces:
      - "application/json"
      parameters: []
      responses:
        200:
          description: "Successful operation"
        400:
          description: "Internal Error"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
  /obfn/{beam_id}:
    put:
      summary: "Update configuration by ID"
      description: "Update operation of resource: beams"
      operationId: "update_configuration_by_id"
      produces:
      - "application/json"
      parameters:
      - name: "beam_id"
        in: "path"
        description: "beam id"
        required: true
        type: "integer"
      - name: "x_offset_angle"
        in: "query"
        description: "X Offset Angle (deg)"
        required: true
        type: "number"
      - name: "y_offset_angle"
        in: "query"
        description: "Y Offset Angle (deg)"
        required: true
        type: "number"
      - name: "wavelength"
        in: "query"
        description: "wavelength"
        required: false
        type: "number"
      responses:
        200:
          description: "Successful operation"
          schema:
            $ref: "#/definitions/Operation"
        400:
          description: "Invalid ID supplied"
      x-swagger-router-controller: "swagger_server.controllers.default_controller"
definitions:
  Operations:
    type: "object"
    properties:
      operations:
        type: "array"
        items:
          $ref: "#/definitions/Operation"
    example:
      operations:
      - beam_id: 0
        x_offset_angle: -90
        y_offset_angle: 90
        wavelength: 1553
      - beam_id: 1
        x_offset_angle: -90
        y_offset_angle: 90
        wavelength: 1553
  Operation:
    properties:
      beam_id:
        type: "integer"
      x_offset_angle:
        type: "number"
      y_offset_angle:
        type: "number"
      wavelength:
        type: "number"
    example:
      beam_id: 0
      x_offset_angle: -90
      y_offset_angle: 90
      wavelength: 1553
